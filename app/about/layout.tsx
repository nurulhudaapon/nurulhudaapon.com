import { MainLayout } from '@/components/layout';
import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'NH | About',
  description: 'About Nurul Huda (Apon)',
};

export default MainLayout;
