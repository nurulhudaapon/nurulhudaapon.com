import { MainLayout } from '@/components/layout';
import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'NH | Ask',
  description: 'Ask Nurul Huda (Apon)',
};

export default MainLayout;
