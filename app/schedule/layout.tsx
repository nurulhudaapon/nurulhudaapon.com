import { MainLayout } from '@/components/layout';
import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'NH | Schedule',
  description: 'Schedule a meeting with Nurul Huda (Apon)',
};

export default MainLayout;
